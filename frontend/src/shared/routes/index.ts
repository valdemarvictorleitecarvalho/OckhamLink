/**
 * Route path for the home page of the application.
 */
export const HOME_ROUTE = "/";

/**
 * Route path for the redirect page, which captures the short code as a URL parameter.
 */
export const REDIRECT_ROUTE = "/:code";
